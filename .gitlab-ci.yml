image: node:20

stages:
  - install
  - test

cache:
  paths:
    - node_modules/

install_dependencies:
  stage: install
  script:
    - npm install

test_backend:
  stage: test
  script:
   - npm test